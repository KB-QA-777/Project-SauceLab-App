import { join } from 'path';

export const ANDROID_APP_PATH = join(process.cwd(), 'app', 'android', 'Android-MyDemoAppRN.1.3.0.build-244.apk')
export const JSON_OUTPUT_DIR = join(process.cwd(), 'reports');